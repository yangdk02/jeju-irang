# 제주아이랑 후속 할 일

## 데이터 정리

- [x] `jeju-irang.csv`의 중복 후보 정리
  - `P341`: `휘닉스 제주 섭지코지`
  - `P342`: `휘닉스제주섭지코지`
  - 두 행의 주소가 `제주특별자치도 서귀포시 성산읍 섭지코지로 107`로 동일함
  - 어느 행을 유지할지 결정한 후 사진·설명·편의시설 등 값이 더 충실한 행으로 병합
  - 병합 전 기존 CSV를 백업하고, 즐겨찾기의 `place_id` 참조 여부도 확인
  - 처리 결과: `P341` 유지, `P342` 삭제, 홈페이지·주차·공간 정보 병합

## 승인·내보내기 점검

- [x] 기존 CSV를 `jeju_irang_master`에 최초 한 번 가져오기
- [x] Google Form에서 테스트용 신규 장소 제안 1건 제출
- [x] `review_queue`에서 VWorld 후보와 승인값 확인
- [x] 테스트 요청을 `APPROVED`로 변경하고 CSV 내보내기 실행
- [x] `jeju_irang_export`, 최종 CSV, `sync_log` 결과 확인
- [ ] 테스트가 끝나면 실제 운영 검수 절차 확정

## Streamlit 앱 연결

- [x] 장소 찾기 페이지에 `＋ 장소 제안하기` 연결
- [x] 상세 페이지에 `✎ 장소 정보 수정 제안` 연결
- [x] 수정 링크에 요청 유형·기존 장소명·위치 단서 사전 입력
- [x] Google Form 설정을 Streamlit Secrets로 분리
- [x] 데스크톱·모바일 화면과 링크 생성 검증
- [ ] 배포 환경의 Streamlit Secrets에 `[google_form]` 설정 복사
